

import javafx.scene.layout.TilePane;


public class Grid{

/*    Element[][] newGrid;

    public Grid()
        {
            //making grid of correct size
            newGrid = new Element[40][20];

            //adding empty elements
            for(int i = 0; i<40;i++)
            {
                for(int j = 0; j<20; j++)
                {
                    Element element = new Element();
                    newGrid[i][j] = element;
                }
            }
        }

*/
}
